# whiteboard-desktop backend — wraps the LogosForge core API in-process.
#
# The core is installed editable (local monorepo, headless API extras only):
#     pip install -e ../../logosforge
# which pulls the core's lean API dependency set (no PySide6).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27
