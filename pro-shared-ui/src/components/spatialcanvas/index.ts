/**
 * Spatial Canvases (design Ticket 03) — implemented from the Claude Design
 * handoff page (canvas mode, 3 frames). The signature spatial surfaces: the
 * narrative Knowledge Graph (story HUD), the free Canvas Plot board, and the
 * Plot-Lane Timeline swimlane board. Each is a standalone <PanelShell> panel
 * with the design's data-screen-label + var(--accent).
 */
export * from "./KnowledgeGraph";
export * from "./CanvasPlot";
export * from "./TimelinePanel";
