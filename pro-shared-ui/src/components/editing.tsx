/**
 * Manuscript & Structure area (design Ticket 02) — implemented for real from the
 * Claude Design handoff. The panels live in ./manuscript; this file re-exports
 * them so `ManuscriptEditor`, `StoryGrid`, `OutlinePanel`, `StructurePanel`, and
 * `NotesPanel` keep their public names.
 */
export * from "./manuscript";
