/**
 * PSYKE Story Bible area (design Ticket 04) — implemented for real from the
 * Claude Design handoff. The panels live in ./bible; this file re-exports them
 * so `PsykeBible` and `PsykeInspector` keep their public names (plus the new
 * RelationGraph, ControllingIdeaCompass, and PsykeConsoleInbox panels).
 */
export * from "./bible";
