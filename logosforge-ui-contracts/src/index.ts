/**
 * @logosforge/ui-contracts — the shared language between the logosforge core and
 * every frontend (Whiteboard + Studio, desktop + web). Types + event names +
 * vocabulary + route map. No logic, no UI, no platform code.
 */
export * from "./types";
export * from "./events";
export * from "./commands";
export * from "./routes";
