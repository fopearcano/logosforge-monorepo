{
  "name": "@logosforge/ui-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts (DTO types, event names, command/enum names, API route map) mirroring the logosforge core API. The single shared language between the Python core and every LogosForge frontend (Whiteboard + Studio, desktop + web).",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "import": "./src/index.ts" }
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
